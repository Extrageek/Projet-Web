import { IScrabbleLetter } from "./../scrabble-letter";
import { Player } from "./../player";
import { expect } from "chai";

describe("Player model should", () => {
    let _username = "Rami";
    let _numberOfPlayers = 2;
    let _socketId = "abc";
    let _score = 0;
    let _letters: Array<IScrabbleLetter>;

    // beforeEach(() => {

    // });

    // it("construct a board correctly", () => {

    // });


});
