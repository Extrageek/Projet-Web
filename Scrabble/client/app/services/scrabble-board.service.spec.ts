import { expect } from "chai";
import { ScrabbleBoardService } from "./scrabble-board.service";

let service : ScrabbleBoardService;

describe("Scrabble grid generation test", () => {

    beforeEach(() => {
        //service = new ScrabbleBoardService;
    });

    it("should correctly validate grid", () => {
        //expect(service.getScrabbleGrid()).to.be.not.be.undefined;
        //expect(service.getScrabbleGrid()).to.have.lengthOf(15);
    });

    it("should correctly return a row length of 15", () => {
       //expect(service.getScrabbleGrid()[0]).to.have.lengthOf(15);
    });

});


