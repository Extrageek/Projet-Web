// import { expect } from "chai";
// import { GameInitSocketHandler } from "./gameInitSocketHandler";
//
// let sinon = require('sinon');
// var alert : any;

describe("Socket handler should generate the appropriate alerts", () => {

    beforeEach(() => {
        // alert = siNon.spy();
    });

    it("invalidPlayerName method from the _socket handler should be called", () => {
        // expect(alert.calledOnce).to.be.true;
    });
});


